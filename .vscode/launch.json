{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Redis Server",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/build/server",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build-debug"
        }
    ]
}