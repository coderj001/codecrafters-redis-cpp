{
  "name": "redis-server-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Redis server using JavaScript and redis-cli",
  "main": "test-server.js",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles",
    "test:cli": "jest tests/cli.e2e.test.js --runInBand",
    "test:all": "npm run test && npm run test:cli",
    "test:watch": "nodemon --exec \"npm run test:all\"",
    "check-cli": "which redis-cli || echo 'redis-cli not found. Please install Redis CLI.'"
  },
  "dependencies": {
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "jest": "^29.6.1"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 20000
  }
}
